input_settings:

  input_dir: inputs
  dataset_dir: scRNA-seq/Specific
  datasets:
  - name: hESC/hESC-TFs-1000-Generator
    cellData: PseudoTime.csv
    exprData: ExpressionData.csv
    trueEdges: refNetwork.csv


output_settings:

  output_dir: outputs
  output_prefix: Specific-hESC-TFs-1000