input_settings:

  input_dir: inputs
  dataset_dir: scRNA-seq/Nonspecific
  datasets:
  - name: mDC/mDC-TFs-1000-Generator
    cellData: PseudoTime.csv
    exprData: ExpressionData.csv
    trueEdges: refNetwork.csv


output_settings:

  output_dir: outputs
  output_prefix: Nonspecific-mDC-TFs-1000