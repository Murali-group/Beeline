input_settings:

  input_dir: inputs
  dataset_dir: scRNA-seq/Nonspecific
  datasets:
  - name: hESC/hESC-TFs-500-Generator
    cellData: PseudoTime.csv
    exprData: ExpressionData.csv
    trueEdges: refNetwork.csv


output_settings:

  output_dir: outputs
  output_prefix: Nonspecific-hESC-TFs-500