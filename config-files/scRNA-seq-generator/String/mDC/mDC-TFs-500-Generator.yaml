input_settings:

  input_dir: inputs
  dataset_dir: scRNA-seq/String
  datasets:
  - name: mDC/mDC-TFs-500-Generator
    cellData: PseudoTime.csv
    exprData: ExpressionData.csv
    trueEdges: refNetwork.csv


output_settings:

  output_dir: outputs
  output_prefix: String-mDC-TFs-500